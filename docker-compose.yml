services:
  daily-kotoba:
    image: ghcr.io/mitchins/daily-kotoba:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      TZ: Australia/Sydney
      KOTOBA_LEVELS: "N5,N4,N3,N2,N1"
    volumes:
      - kotoba-data:/data

volumes:
  kotoba-data:
